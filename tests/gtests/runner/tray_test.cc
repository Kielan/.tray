/* This file is intentionally left blank. It is necessary for CMake to have a source file for each
 * executable. However, the tray_tests test runner only comprises of statically linked lib
 * files, including its main fn.
 *
 * See source/tray/kernel/CMakeLists.txt for an example of how to add unit tests to the test
 * runner. */
